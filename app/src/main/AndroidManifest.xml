<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gtbit.retro06.www.sikkimelectricityboard">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"
            android:label="@string/app_name">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".Payment"
            android:label="Payment Portal"/>
        <activity android:name=".Bill"
            android:label="Bill Details"/>
        <activity android:name=".UserHistory"
            android:label="History"/>
        <activity android:name=".UserComplaint"
            android:label="Complaints And Queries"/>
        <activity android:name=".UserFeedback"
            android:label="Feedback"/>
        <activity android:name=".UserQuery"
            android:label="Complaints and Queries"/>
        <activity android:name=".CircularsNotices"
            android:label="Circular And Notices"/>
        <activity android:name=".SafetyTips"
            android:label="Safety Tips"/>
        <activity android:name=".NearByCenter"
            android:label="Centers near Me!"/>
    </application>

</manifest>